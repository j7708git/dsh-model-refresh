# DSH 模型清單智慧更新工具（dsh-model-refresh）開發文件

- 版本：v1.3
- 日期：2026-09-06
- 狀態：**M1 已上線生效；M2 已修完兩個真機 bug（見進度日誌）並通過真實 cordis 主機重現驗證，待使用者「最後一次」重啟載入修復版**。詳見下方進度日誌與 §10。
- 目標讀者：本工具的實作者（含 DSH agent 本身）

### 進度日誌

| 日期 | 事件 |
|---|---|
| 2026-09-05 | v1.0 定稿：需求確認（pins 對照表、免費池上限 10、$1 門檻規則甲）、資料端點與 DSH 機制實測完成（§2/§3） |
| 2026-09-05 | **M1 完成**：core＋CLI（plan/apply/status/rollback、備份、回滾、離線模式），26/26 單測，真實 dry-run 新增 32/移除 0 |
| 2026-09-06 | **M1 正式套用**：寫入 `~/.dsh/settings.yaml`（openrouter 3→22、nous-api 8→21），冪等驗證通過；此即目前生效的模型清單 |
| 2026-09-06 | **M2 完成並安裝**：plugin 殼（prefs 命名空間/timer/mutate 樂觀鎖寫入）＋漂移處理（手動新增收編、手動刪除抑制、pin 刪除恢復），33/33 單測 |
| 2026-09-06 | **M2 事故與修復（v0.2.1）**：首次重啟崩潰（`exports` 缺 `"."` 主入口 → 裸名 import 失敗）；hermes-agent 移除掛載止血後，本方修復並加三道防呆（打包守衛＋fake cordis 掛載冒煙測試＋profile 裸名 import 必驗規程），重裝完成、36/36 綠、dump-config 確認掛載。**待第二次重啟生效。** |
| 2026-09-06 | **M2 第二次事故（掛載成功但無心跳）**：使用者第二次重啟後 dsh 正常、但 `~/.dsh/model-refresh/` 時間戳證明第一輪 tick 根本沒跑。單測全綠卻真機失效 → 在程序內起**真實 cordis 主機**（`new Context()`＋官方 plugin-loader，baseUrl=profile 目錄）完整重現，抓到連環兩 bug：① cordis `ctx.inject` 傳給回調的 context **在回調返回後即失效**，tick 稍後經 `sctx.settings` 延後取用直接拋「inactive context」並被無聲吞掉（改法：回調有效期間先捕獲服務實例，之後全走實例）；② 修 ① 後掛載尾段仍缺「已載入」日誌 → 發現 **cordis 4 沒有 `ctx.dispose()`**——teardown 慣例是「plugin effect 回傳 disposer 函式」，誤用不存在的 API 拋 TypeError（假 ctx 單測自己發明了 dispose，反而掩護此 bug；已改為鏡像真實介面＋斷言回傳 disposer＋斷言「已載入」日誌）。同時給日誌加 console 兜底，**吞錯從此不可見**的時代結束。新增發布前深檢腳本 `scripts/verify-cordis-mount.mjs`（真 loader＋fetch 計時探針）。真機重驗：「已載入」→ fetch（OR 171ms／Nous 302ms）→ mutate 雙 route → state 落盤，全鏈綠；單測 36/36 |
| 2026-09-06 | **v0.2.2 獨立審查＋防禦性修補**：外部審查對照 DSH 真實 settings 服務契約（register/describe/mutate/scope.watch 簽名、`agent-default-model` 命名空間）全數吻合，單測 8 檔全綠；隨手修四個次要問題：① `scope.watch()` 回傳的 disposer 未保留 → watcher 在 plugin 卸載後仍存活（現在併入 teardown disposer）；② `reschedule()` 內 `scope.get()` 無 try/catch（watch 回調拋錯行為不可控，現防護並 fallback 現有計時器）；③ `atomicWriteText` 的 Windows 重試用 busy-wait 空轉阻塞 host 事件循環（改 async sleep）；④ CLI `rollback` 不回滾 `state.json`，常駐 plugin 下輪會重套新計畫（現於輸出明確警告並提示先 `enabled: false` 或刪 state） |
| 2026-09-06 | **v0.2.4**：受管條目寫入 `reasoningEfforts` 思考能力欄位（修 nous-api 無法選思考強度） |
| 2026-09-06 | **M4 規劃定稿**（`M4-Web設定頁規劃.md`）：實地盤點 DSH web 設定頁機制——Plugins 分區渲染 =「served namespaces ∩ 已註冊卡片」交集、卡片全為手寫 React（無 schema 自動生成表單）、第三方插件需 `dsh.client` 宣告瀏覽器半部；手動刷新定為**共用常駐 tick**（否決 CLI apply 子程序路徑——檔案直寫繞過 revision fence）。方案：宿主 route（`ctx.webServer.register`＋better-sidebar 同款 `isTrustedApiRequest` 圍欄）＋ `settings.plugin.item` keyed 卡片。 |
| 2026-09-06 | **M4-A 完成（宿主 HTTP 面）**：新增 `src/plugin/http.js`——`GET /model-refresh/api/status`（prefs＋lastRunAt/lastAppliedAt＋各 route 條目數）與 `POST /model-refresh/api/refresh`（共用 tick：單飛旗標忙線回 `busy`、`mutateWithRetry` 樂觀鎖照舊；手動 `force` 繞過 enabled 閘門，支援「停用自動、保留手動」）；`tick()` 改回傳結果物件（定時路徑忽略之，行為不變）。單測 48/48（新增 http.test.js 10 項＋mount 擴充 2 項）；真機深檢 `verify-cordis-mount.mjs` 增掛 FakeWebServer，HTTP STATUS/REFRESH/FENCE 三探針全綠。**已知觀察項**：連續兩輪對同一 route 出現「同集合異順序」重寫（免費池 tie-break 受目錄 API 返回順序影響）——M3 觀察期既有行為，非 M4 引入，暫不修。待重啟 `dsh web` 生效。 |
| 2026-09-06 | **M4-B 完成（Web 設定頁卡片，v0.3.0）**：`package.json` 增 `dsh.client` 宣告（`platform: "web"`，inject `dsh-client-ui-slots`＋`dsh-client-ui-settings`）與 `exports["./client"]`；新增 `src/client.js`——**手寫 lazy-CJS factory 格式**的瀏覽器半部（零構建鏈，規劃中的 tsdown 構建鏈省略，與官方 bundle 同信封 `window.__ModuleLoader__.load({id, factory})`），掛 `settings.plugin.item` keyed slot（key=`dsh-model-refresh`），卡片含三 prefs 的 staging 表單（revision-fenced `scope.mutate`、覆寫徽章＋重設=即時 `unset`）、狀態列（M4-A status route）、立即刷新按鈕（busy/disabled/結果全回饋）。新增 `test/client-card.test.js`（vm 執行 bundle＋stub React/slots/settingsScope 驗 slot 註冊契約、save→mutate op 形狀、absent/ready 兩渲染態），單測 53/53；裸名 import 解析層複驗通過、真機深檢宿主面無回歸。**待重啟 `dsh web` 做第 4 層 UI 驗證**（卡片可見性只能真機驗）。 |
| 下一步 | 使用者**重啟** `dsh web`（載入 M4-A+M4-B）→ 按序跑四層驗證：① profile 裸名 import（已離線複驗 ✓）② `node scripts/verify-cordis-mount.mjs`（HTTP 三探針 ✓）③ 線上心跳：+90s `state.json` mtime 刷新、host 日誌「已載入／HTTP routes 已註冊」④ **UI 層**：設定 → Plugins → Plugin configuration 出現卡片，改 prefs 熱生效、立即刷新全鏈回饋。之後進入 §12 觀察期；M3（候補滯回觀察、Nous `original` 折後價預警、LLM judge 選配）與 M4-C（規則參數 prefs 化、locale 字典、變更通知 badge）並行推進 |

---

## 0. TL;DR 評估結論

**可行，且成本可控。** 三個關鍵前提全部實測成立：

1. **DSH 端**：模型清單由 `~/.dsh/settings.yaml` 的 `llm-pi-ai.providers.<route>.models` 決定，DSH **每次請求重讀該檔、熱生效、不需重啟**；寫入時有 schema 驗證器把關（無效區段保留最後良好值）。外部程式只要做「原子寫入 + 先驗證後寫」即可安全接管這段設定。
2. **資料源端**：Nous 與 OpenRouter 的模型目錄都有**免认证的公開 JSON API**（見 §3），且附帶價格、上下文長度、工具呼叫支援、推理能力、甚至 **Artificial Analysis 能力指數（benchmarks 欄位）**——「篩選 $1/M 以下且能力相當或更強」可以用資料驱动的規則實作，不需要爬網頁。
3. **慣例參考**：Nous 自家的 `hermes-agent`（Python）已實作幾乎相同的問題（OpenRouter + Nous Portal 雙目錄、免費層判定、TTL 快取、last-known-good 回退），其 `hermes_cli/models.py` 是可借鑑的成熟先例（附錄 C）。

建議形態：**TypeScript 核心函式庫 + DSH 常駐 plugin（主）＋ CLI 除錯模式（副）**。詳見 §5。

---

## 1. 需求理解與範圍

### 1.1 三層需求

| # | 需求 | 解讀 | 對應規則 |
|---|------|------|----------|
| R1 | 免費模型自動更新 | Nous 與 OpenRouter 每天/每陣子輪替免費模型；工具自動把「當下免費」的模型加進 DSH 可選清單，過期的自動移除 | R1（§7.1） |
| R2 | 常駐付費模型（pins） | 永遠保留：DeepSeek V4 Flash Latest、DeepSeek V4 Vision、GLM 5.3 Flash、Qwen 3.7 Flash、Qwen 3.8 Flash 這 5 個（或各 provider 上的等價 id） | R2（§7.2） |
| R3 | 智慧候選（3–5 個） | 從兩家目錄篩出輸入**與**輸出皆 ≤ $1/1M tokens、能力與 pins 相當或更強的模型，取 3–5 個進清單；汰換要有依據、避免反覆震盪 | R3（§7.3） |

「智慧更新」= 不是無腦全量覆蓋，而是：品質閾值過濾 → 能力評分 → 滯回（hysteresis）汰換 → 差異化寫入 → 失敗回退。

### 1.2 不做什么（範圍外）

- 不改 `agent-default-model`（預設模型仍由使用者決定；工具只可「提醒」預設模型已被下架）。
- 不動 `.credentials.yaml` / API keys（金鑰本来就走 `apiKeyEnv` 參考，不入設定檔）。
- 不接管非 openrouter / nous-api 的 route（如 deepseek 官方）。
- M1 階段不做 LLM 判讀候選能力（先用資料規則；LLM judge 列為 M3 選配，見 §7.4）。

### 1.3 pins 的實際 id 對照（2026-09-05 實測）

| 使用者用語 | 目錄中的實際 id | OpenRouter 現價 in/out（$/M） | Nous 現價 in/out（$/M） | 備註 |
|---|---|---|---|---|
| DeepSeek V4 Flash Latest | `~deepseek/deepseek-v4-flash-latest` | 存在（pi-ai 靜態目錄與線上皆有 `~` 路由器別名） | $0.04 / $0.08 | 「~」前綴是 provider 的 auto-latest 路由器：*always redirects to the latest model in the DeepSeek V4 Flash family*，兩家 id 相同 |
| DeepSeek V4 Vision | `deepseek/deepseek-v4-flash-vision-exp` | $0.22 / $0.66（週末同價） | $0.17 / $0.52（折後） | ✅ 2026-09-05 使用者確認：此即「DeepSeek V4 Vision」；若日後目錄出現非 exp 正式版，依 §7.2 別名規則自動升級 |
| GLM 5.3 Flash | `z-ai/glm-5.3-flash` | $0.075 / $0.25 | $0.06 / $0.20 | AA 指數：intelligence 46.2 / coding 71.5 / agentic 51.5（本表能力基準錨點） |
| Qwen 3.7 Flash | `qwen/qwen3.7-flash` | $0.03–0.2 / $0.13–0.8（階梯價） | （視線上目錄） | 多模態（text+image+video→text） |
| Qwen 3.8 Flash | `qwen/qwen3.8-flash` | $0.15 / $0.47 | $0.12 / $0.376（折後） | 即本 harness 目前執行中的模型 |

> 對照表是可設定的（`pins.yaml`），因為 id 會隨供應商改名。解析規則：精確 id → 別名候選 → `~<family>-latest` → 都找不到時**發警示並保留舊條目**，絕不靜默刪除 pin。

---

## 2. 現況盤點（已驗證的事實）

### 2.1 使用者目前的設定方式

`C:\Users\denny\.dsh\settings.yaml`（實讀）：

- `llm-pi-ai.providers.openrouter`：掛在 pi-ai 內建目錄 route 上，`models:` 列了 3 條（deepseek v4 flash、v4 pro、`openrouter/free` 路由器）。**`models` 清單是「整份取代」內建目錄**，不是追加——這正是工具要管理的區域。
- `llm-pi-ai.providers.nous-api`：手工宣告 route（`api: openai-completions`、`baseURL: https://inference-api.nousresearch.com/v1`），`models:` 列了 7 條手工查好的条目。存在 `.bak-prenous` 備份檔 → 使用者已有手動備份習慣，工具要把這流程自動化。
- 兩條 route 各自 `apiKeyEnv: OPENROUTER_API_KEY / NOUS_API_KEY`，免金鑰的公開目錄 API 足以完成本工具的抓取（金鑰只在真正呼叫模型時用）。

### 2.2 DSH 載入機制（決定工具怎么写設定才安全）

來源：`dsh-llm-pi-ai` 套件 README 與 `dsh-settings`/`dsh-settings-file` 套件文件（路徑見附錄 B）。逐條摘錄並對應到設計決策：

| DSH 行為（實測/文件確認） | 對工具的意義 |
|---|---|
| 設定「per-operation 重讀」，改檔後下一次請求即生效、**不需重啟** | 常駐刷新可行；CLI 模式也不用擔心 DSH 重啟成本 |
| 存檔的區段若驗證失敗 → **保留 last-good 並告警**，不會讓整個命名空間失效 | 寫壞的機率被驗證器兜住；工具仍要自己先驗證（外部寫檔繞過 `settings.mutate` 的即時拒絕） |
| `models` 清單整份取代 route 的目錄；條目未填的欄位**繼承 pi-ai 內建目錄同 id 的條目**（僅對 openrouter 這種內建 route 有效） | 對 openrouter：老模型可只寫 `id`；**內建目錄沒收的新模型必須填 `contextWindow`/`maxTokens`** |
| `nous-api` 是 hand-declared route，**無內建目錄可繼承**；未填容量走 route 預設（262144 / 32768） | Nous 上每個條目都要由工具填入 `contextWindow`、`maxTokens`，vision 模型還要多寫 `input: [text, image, ...]`，否則圖片請求會被拒 |
| pi-ai 內建目錄是**打包時的靜態快照**（本機版 generatedAt 2026-08-28），「A route's catalog never refreshes itself」 | 這是本工具存在的理由：DSH 官方刻意不做自動目錄刷新，留給外掛做 |
| `dsh-settings-file`：檔案可直接編輯、熱重載；YAML 服務端寫入保留註解與未動節點 | 工具用 `yaml` 套件做「文檔級」修改（只動受管節點），與使用者手改相容 |
| `settings` 服務提供 `ctx.settings.update/replace/mutate(ns, …, expectedRevision)`，並跑擁有者註冊的驗證器 | **plugin 模式**可用它做帶 revision 樂觀鎖的跨命名空間寫入（優於直接寫檔） |
| `dsh plugin --profile web add <pkg>`、profile bundles、dsh-plugin-hub 市集 | 有現成分發管道；`dsh-better-sidebar`、`dsh-smart-approval` 即同類先例 |
| `dsh-schedule` 只是 session 內提醒工具，不是排程守護者 | 排程要嘛用 plugin 內建 timer（`@deepseek-ai/cordis-plugin-timer` 可用），要嘛用 Windows 工作排程器 |

---

## 3. 資料源驗證結果（全部實測通過，無需认证）

### 3.1 OpenRouter

| 項目 | 值 |
|---|---|
| 目錄 API | `GET https://openrouter.ai/api/v1/models` |
| 過濾參數 | `?output_modalities=text`、`?supported_parameters=tools`（實測 HTTP 200、參數生效） |
| 認證 | 不需要（讀目錄） |
| 免費判定 | `pricing.prompt == 0 && pricing.completion == 0`；免費變體 id 帶 `:free` 後綴（另有 `synthesizedFreeVariant: true` 標記） |
| 能力欄位 | `benchmarks.artificial_analysis.{intelligence_index, coding_index, agentic_index}`、`benchmarks.design_arena[]`、`description`、`supported_parameters`、`reasoning.supported_efforts`、`context_length`、`top_provider.max_completion_tokens` |
| 干擾項 | `:batch`（離線批價）、`:free` 變體、embeddings/voice 模型——全部要在 normalizer 排除或降權 |

### 3.2 Nous

| 項目 | 值 |
|---|---|
| 全目錄 API | `GET https://inference-api.nousresearch.com/v1/models`（免认证實測 200；回傳 OpenRouter 同構式的豐富目錄，含 `:free` 變體與折後價 `original` 欄） |
| 推薦/輪替 API | `GET https://portal.nousresearch.com/api/nous/recommended-models`（免认证實測 200）：`freeRecommendedModels[]`、`paidRecommendedModels[]`、`freeRecommendedVisionModel`、`freeRecommendedCompactionModel`；每項含 `modelName`、`source`（`local` = Nous 自有 / `openrouter` = 鏡像）、`tokenPrice`、`updatedAt`（實測更新到當天） |
| 免費判定 | 同 OR 式（`pricing` 為 0）；另 `freeRecommendedModels` 直接反映 Portal 頁面上的免費區 |
| 特殊 id | `~<vendor>/<family>-latest` 路由器別名（如 `~deepseek/deepseek-v4-flash-latest`，tokenizer 標為 `Router`） |
| 網頁 | `portal.nousresearch.com/models` 是 React 前端，資料即來自上述兩支 API；工具不需要碰網頁 |

**免費池雙軌**：Portal 推薦清單代表「官方正在推的免費輪替」；`/v1/models` 零價代表「此刻實際可調的免費」。工具取兩者聯集，但寫入前一律用全目錄 API 复核零價與 `supported_parameters ∋ tools`，避免推薦檔落後造成的殭屍條目。

### 3.3 節流與容錯

- 兩家都是靜態 JSON、無分頁（一次全量約 100–500 KB）；每日 1–4 次抓取完全禮貌。
- 必須實作：HTTP 重試（指數退避）、schema 驗證、**last-known-good 快取**（抓不到時沿用上次成功結果，hermes-agent 同款策略）。

---

## 4. 名詞與資料模型

抓取後一律歸一化為 `CatalogEntry`，之後的規則只看這個結構：

```ts
interface CatalogEntry {
  provider: 'openrouter' | 'nous';
  id: string;                 // 原樣送給 API 的 model id（含 ~、:free 後綴）
  displayName: string;        // 寫入 settings 的 name
  inputPriceUSDPerM: number;  // pricing.prompt * 1e6
  outputPriceUSDPerM: number; // pricing.completion * 1e6
  contextWindow: number;      // top_provider/context_length ?? context_length
  maxTokens: number;          // top_provider/max_completion_tokens
  modalities: string[];       // architecture.input_modalities
  supportsTools: boolean;     // supported_parameters ∋ 'tools'
  reasoning?: { mandatory: boolean; efforts: string[] };
  free: boolean;              // prompt==0 && completion==0
  variant: 'base' | 'free' | 'batch' | 'router' | 'other';
  benchmarks?: { intelligence?: number; coding?: number; agentic?: number };
  huggingFaceId?: string; description: string; created: number;
}
```

> 注意 `pricing.prompt` 是「每 token 美元字串」；一律用十進位字串→整數換算，避免浮點誤差在 $0.999 邊緣誤判。

---

## 5. 形態評估：CLI、DSH plugin、還是混合？

| 方案 | 說明 | 優點 | 缺點 |
|---|---|---|---|
| A. 獨立 CLI + Windows 工作排程器 | Node/TS 指令碼，每 N 小時跑一次，直接原子改寫 `settings.yaml` 受管節點 | 最簡單、與 DSH 生命週期無關、除錯容易、DSH 沒開也能更新 | 非「常駐」體驗；寫檔繞過 settings 服務（需自行驗證 + 備份）；手改與自動改的合併要小心 |
| B. DSH cordis plugin（常駐） | 掛進 web profile；own 一個 `model-refresh` 設定命名空間；內部 timer 週期抓取；用 `ctx.settings.update('llm-pi-ai', …, expectedRevision)` 寫入 | 熱生效、驗證器+樂觀鎖天然保護、可註冊自己的 schema/UI 命名空間、可走 hub 市集分發、与 DSH 生態一致 | 只在 DSH web 執行時更新（對本使用者=常開，影響小）；開發需遵守 cordis 插件慣例 |
| C. **核心庫 + B 為主 + A 為輔（建議）** | 抓取/規則/規劃/寫入全部放純 TS 核心包（無 DSH 相依、可單測）；plugin 與 CLI 只是兩個薄殼 | 同一套邏輯：plugin 常駐跑、CLI 供手動 `refresh --dry-run` 與排程備援；未來若 DSH 官方版做目錄刷新，淘汰殼保留核心也很容易 | 多一個包邊界（但就是 monorepo 兩行設定） |

**推薦 C，M1 先交付 CLI（含核心庫），M2 加 plugin 殼。** 理由：CLI 最快落地、風險最低；plugin 提供使用者真正想要的「常駐自動」體驗時，核心邏輯已驗證過。

### 5.1 組件圖

```
          ┌────────────────────────────────────────────────┐
          │            model-refresh-core (TS)             │
          │                                                │
 OR API ──►│ fetchers ─► normalize ─► rules ─► plan ─► apply│──► ~/.dsh/settings.yaml
 Nous API ►│   (R1 免費)  (R2 pins)  (R3 候選) (diff)  (驗證+  ▲
          │                                    │        原子寫+備份) │
          │                          state.json│(last-known-good、  │
          │                                    │ 快取、滯回記帳)     │
          └────────────▲───────────────▲───────┘                │
                       │               │                         │
              [殼 A] CLI 指令      [殼 B] DSH plugin        DSH llm-pi-ai
              (node CLI, 排程/手動)  (cordis timer +         每請求重讀設定
               `--dry-run` diff)     ctx.settings.update)
```

### 5.2 建議 repo 結構

```
dsh-model-refresh/
├─ packages/core/          # 純函式庫：fetch/normalise/rules/plan/write
│  ├─ src/{fetchers,normalize,rules,capability,planner,writer,state}.ts
│  └─ test/fixtures/       # 兩家 API 的快照 JSON（離線單測用）
├─ packages/cli/           # commander：refresh / plan / validate / rollback
├─ packages/plugin/        # cordis 插件：model-refresh（timer + settings namespace）
├─ config/pins.example.yaml
└─ docs/（本文件）
```

---

## 6. 設定檔寫入契約（核心設計）

1. **所有權劃分**：`settings.yaml` 中 `llm-pi-ai.providers.openrouter.models` 與 `.nous-api.models` 兩個清單**完全由工具擁有**；使用者的個人偏好（pins、候補偏好、開關）寫在自己的 `model-refresh:` 命名空間/設定檔，而不是直接改受管清單。避免「使用者手改 → 下次刷新覆蓋」的互踩。
   - 工具在受管清單每條 name 不後加標籤（不影響 id）；在 `model-refresh.state` 記錄來源（`pin|candidate|free|imported`），`imported`（首次接管時使用者既有的條目，如 `openrouter/free`）享有「預設保留」待遇。
2. **寫入路徑**：
   - CLI：`yaml` 套件 parse → **只替換受管節點** → `dsh-atomic-write` 風格的 tmp+rename 原子替換 → 同目錄留 `.bak-<n>` 最近 5 份。DSH 熱重載，無需通知。
   - plugin：`ctx.settings.update('llm-pi-ai', { providers: { openrouter: { models } , nous-api: {...} } }, expectedRevision)`——經 `assertServiceable` 驗證、併發寫入串行化、失敗自動保留 last-good。
3. **自我驗證**（雨條路徑共用，寫入前）：id 非空且唯一、`contextWindow/maxTokens` 正整數、nous route 每條目欄位自足、總條目數上限（默认每 route ≤ 24）、與上版 diff ≤ 合理變動量（超過需 `--force`）。
4. **回滾**：`refresh --rollback` 用 state.json 記的上一版快照直接還原。

### 6.1 產物範例（節錄）

```yaml
llm-pi-ai:
  providers:
    openrouter:
      apiKeyEnv: OPENROUTER_API_KEY
      models:
        # --- pins（R2，永遠保留） ---
        - id: ~deepseek/deepseek-v4-flash-latest
          name: "DeepSeek V4 Flash Latest"
        - id: deepseek/deepseek-v4-flash-vision-exp
          name: "DeepSeek V4 Flash Vision (exp)"
          contextWindow: 1048576   # 新於內建目錄快照的模型：顯式元資料
          maxTokens: 384000
          input: [text, image, video]
        # ...glm/qwen pins...
        # --- candidates（R3，滯回汰換） ---
        - id: poolside/laguna-s-2.1
          name: "Poolside Laguna S 2.1"
        # --- free（R1，自動增減，name 加標籤） ---
        - id: thinkingmachines/inkling-small:free
          name: "[Free] Thinking Machines Inkling Small"
          contextWindow: 1048576
          maxTokens: 262144
    nous-api:
      # hand-declared route：每條目一律帶全元資料（無內建目錄可繼承）
      models:
        - id: qwen/qwen3.8-flash
          name: "Qwen: Qwen3.8 Flash"
          contextWindow: 1000000
          maxTokens: 131072
```

---

## 7. 更新規則（演算細節）

### 7.1 R1 免費模型自動更新

入選條件（全部 AND）：
- `variant ∈ {base, free}`；對 `:free` 變體，若同 id 的非免費版也在池內則**收錄 `:free` 版**（用戶要的是零費用的那條路由）；
- `free == true`（prompt 與 completion 皆 0；以全目錄 API 的即時 `pricing` 為準，推薦清單只作為來源提示）；
- `supportsTools == true`（agent 場景硬需求；hermes-agent 同款過濾）；
- `output_modalities ∋ text` 且非 embeddings/voice（排除 `voyage-code-4` 這類）；
- `contextWindow ≥ 262144`（可設定；免費池常見小模型，避免清單塞滿不可用選項）。

數量與汰換：
- 每個 provider 上限 `freeMax`（預設 10，依 2026-09-05 實測池大小合理：OR 目前 ≥7 條、Nous Portal 推薦 7 條）；
- 超出上限時按「能力分數（§7.3 同式，缺指數用 ctx/工具/推理由 description 關鍵詞）降序」保留；
- 名稱統一加 `[Free] ` 前綴，清單排序固定在末段。
- 每日輪替的處置：掉出免費的條目直接移除（它們是工具拥有的清單段）；若被移除的 id 恰為當前 `agent-default-model`，**不移除、保留並在報告中警示**（保護進行中工作）。

### 7.2 R2 pins 解析

- 依 §1.3 對照表逐 provider 解析；解析不到 → 警示 + 保留上版條目 + 通知（不阻塞其他更新）。
- pins 的元資料同樣從目錄抓取；id 是 `~` 路由器時照 pi-ai 靜態目錄條目繼承（OR route），Nous route 手工帶全。
- 使用者在 `pins.yaml` 可加/減 pin（名稱→候選 id 列表，支援 regex）。

### 7.3 R3 候補評選（$1 以下、能力相當或更強）

**第一步：硬過濾**
- `inputPrice ≤ 1.0 && outputPrice ≤ 1.0`（用**標準價**，不用 `:batch`/週末折扣價；階梯價模型採**規則甲（已確認）**：第一檔 ≤$1 即合格，較高檔位破 $1 僅在報告註記）；
- `supportsTools`、text 輸出、`contextWindow ≥ 262144`；
- 排除 pins、免費條目、`preview/exp` 字樣（除非使用者放行）、同家族重複（同一 canonical_slug 只留最強一條）。

**第二步：能力評分**（相對基准 = pins 的 AA 指數中位數；實測基準 intelligence≈41–46、coding≈69–72、agentic≈42–52）
- 有 AA 指數：`score = w_i·intelligence + w_c·coding + w_a·agentic`（預設 0.4/0.4/0.2，歸一化至基準=100）；
- 無指數：以代理訊號給「未驗證」分並可設定是否入選——Terminal-Bench/設計竞技场 ELO 等 `description`/`benchmarks` 內數據、參數規模（MoE active params）、`reasoning.efforts` 數量、context 大小；
- 入選門檻：`score ≥ 基準 × 0.95`（預設，可調）；
- 取 top `N`（預設 4，範圍 3–5）。

**第三步：滯回汰換**（反震盪，關鍵）
- 現有候補被挑戰者取代需同時滿足：挑戰者分數 ≥ 在位者 × 1.10，且在位者已連續 2 個刷新週期落後；
- 新進者有 7 天冷卻觀察（記帳在 state.json），期間不落下的排名變動一律不套用；
- 價格變動（漲破 $1）立即出局（硬條件無滯回）。

**第四步（選配，M3）：LLM judge**
- 對「分數接近門檻 ±10%」的邊界候選，用當前 DSH 模型跑一次結構化判讀（輸入：description、ctx、價格、efforts；輸出：`{verdict: in|out, reason}`，低溫、限流、結果進 state 快取 7 天）。規則層仍是最終把關，LLM 只調邊界。

### 7.4 排程與事件

- plugin 模式：啟動即跑一次，之後每 `intervalHours`（預設 12）；`ctx.settings.watch` 偵測到使用者改動受管節點 → 重算擁有權並合併；
- CLI/排程模式：Windows 工作排程器每日固定時刻；`--dry-run` 只印 plan；
- 兩家 API 任一失敗：用 last-known-good；雙失敗且無快取：本輪不寫入。

---

## 8. 可觀測性

- 每次 run 輸出機器可讀報告 `state/last-run.json`：抓取样本時間、各規則命中清單、diff（added/removed/kept+理由）、寫入結果；
- 變動通知：plugin 模式在 DSH 內以 log + `llm/refresh` 自訂事件呈現；最簡版 = 寫一個 `CHANGES.md`（人讀的表格式摘要：誰進/誰出/為什麼）；
- CLI `status` 子命令：顯示目前清單、各條目來源規則、下次刷新時間。

## 9. 安全與隱私

- 目錄抓取全程免金鑰；不讀 `.credentials.yaml`；
- API 回傳視為不可信外部資料：schema 白名單解析，字串欄位僅作顯示與 id 透傳，不 eval、不拼路徑；
- 寫檔限定 `~/.dsh/settings.yaml` 受管節點；備份保留在 `.dsh/backups/model-refresh/`；
- 若未來走認證端點（用戶私有目錄），金鑰從 `apiKeyEnv` 同名環境參考取得，不另存。

## 10. 里程碑

| 里程碑 | 內容 | 驗收 |
|---|---|---|
| M0 勘測（✅ 已完成） | 本文 §2/§3 的實測 | — |
| M1 核心+CLI（✅ 已完成 2026-09-05；✅ 已於 2026-09-06 正式套用至 ~/.dsh/settings.yaml，備份於 ~/.dsh/model-refresh/backups/） | fetchers/normalize/R1/R2/R3/寫檔（plan 乾跑、preview-settings.yaml、備份、回滾、離線模式）＋ 26 項單測全綠；實測 dry-run：新增 32、移除 0 | 達標：pins 5/5 兩 route 解析、首次接管僅新增、未受管行零刪除、family 去重生效（0731/1.2-contributor 排除）；套用後安心驗證：再跑一次 plan 與實際檔案 22/22、21/21 完全一致（冪等） |
| M2 plugin 殼（✅ 代碼完成並通過真實 cordis 主機驗證，2026-09-06；待最後一次重啟載入） | `src/plugin/`：prefs 命名空間 `dsh-model-refresh`（enabled/intervalHours/initialDelaySeconds）+ timer + `ctx.settings.mutate` 路徑寫入（樂觀鎖＋衝突重試）；drift 處理（使用者手動新增→imported 收編、手動刪除→抑制、pin 被刪→自動恢復）；實測 `dsh plugin --profile web add <path>` 安裝＋`--dump-config` 確認掛載 | 36/36 單測綠。**教訓鏈三次**：①exports 缺 `"."` 主入口→開機崩潰；②`ctx.inject` 回調 context 回調返回後失效→tick 静默死；③cordis 4 無 `ctx.dispose()`（teardown=回傳 disposer）→掛載尾段 TypeError。②③皆「假 ctx 單測全綠、真機不活」型，已以 `scripts/verify-cordis-mount.mjs`（真 loader 重現）＋單測鏡像真介面封殺；重啟後驗證以 `state.json` 時間戳為準（掛載 ≠ 心跳） |
| M3 完整化 | 候選滯回實戰觀察、價格改列警報、可選 LLM judge | 兩週觀察期內候補震盪 ≤1 次 |
| M4-A 宿主 HTTP 面（✅ 代碼完成 2026-09-06，待重啟載入） | `src/plugin/http.js`：`GET /model-refresh/api/status`＋`POST /model-refresh/api/refresh`（共用 tick 單飛＋mutate 樂觀鎖；isTrustedApiRequest 圍欄 fail-closed）；`tick()` 回傳結果物件；verify 腳本三 HTTP 探針 | 單測 48/48；真機深檢 HTTP STATUS/REFRESH/FENCE 全綠；重啟後線上可 `Invoke-RestMethod` 打通兩條 route |
| M4-B Web 設定頁卡片（✅ 代碼完成 2026-09-06，待重啟做 UI 層驗證） | `dsh.client` 宣告＋`src/client.js`（手寫 lazy-CJS factory，免構建）：`settings.plugin.item` 卡片（enabled/intervalHours/initialDelaySeconds staging 表單＋覆寫標記/reset＋狀態列＋立即刷新按鈕） | 四層驗證鏈：解析 ✓（離線複驗）、掛載心跳 ✓（真機深檢宿主面無回歸）、單測 ✓（53/53，新增 client-card 5 項）、UI 層 ⏳ 待重啟真機驗證 |
| M4-C 選配 | 規則參數 prefs 化（需 cfg 每輪重讀重構）、locale 字典、變更通知 badge、status 回傳 warnings 持久化 | 另立驗收 |

> M1 實作偏离記錄：採「單一套件、ESM + JSDoc 註型別」而非 monorepo＋TS（sandbox/依賴面最小化；`src/core` 與 `src/cli` 邊界已预留 M2 抽包條件）。每個受管條目一律寫入完整元資料（與使用者現有手工風格一致），未依賴 pi-ai 靜態目錄繼承。候選家族去重以 `familyOf()`（去版本數字/日期後綴/`-latest`）取代原稿的 `canonical_slug` 分組，可把「同一模型的舊快照版」視為同族排除。
>
> **M2 安裝發現**：官方 `dsh plugin add <本地路徑>` 對 `link:` 套件不會自動把它追加進 profile 的 `dsh.profile.bundles`（只有 registry 套件會自動對帳），需手動補一條 bundle 元素；補上後 `--dump-config` 驗證 bundle patch 正常合併。插件本體只 import `schemastery`＋自家核心（不直接 import `@deepseek-ai/*` 執行期模組），因此 link 安裝與 peer 警告皆不影響運行。
>
> **M2 事故記錄（2026-09-06 重啟崩潰）**：`exports` 只列子路徑、缺 `"."` 主入口 → cordis 開機以裸套件名 import bundle 時 `ERR_PACKAGE_PATH_NOT_EXPORTED`，整個 web tree 起不來（由使用者的 hermes-agent 先移除掛載止血）。修復：`main`＋`exports["."]` → `src/plugin/index.js`；防範：打包守衛單測＋「安裝後在 profile 目錄重現裸名 import」列為必驗規程（README）。另 `apply(ctx, config)` 增加 `config.fetcherImpl` 測試注入口、`loadConfig` 支持 `DSH_MODEL_REFRESH_STATE_DIR/SETTINGS` 環境覆寫，讓 mount 冒煙測試完全離線可重現。重裝後複驗：profile 目錄裸名 import OK、`--dump-config` 見掛載、36/36 單測綠。
>
> **M2 事故記錄 #2（2026-09-06 第二次重啟：掛載活、心跳死）**：dsh web 正常啟動、dump-config 有掛載，但 `~/.dsh/model-refresh/` 全檔時間戳未動 → tick 從未成功。以 `scripts/verify-cordis-mount.mjs`（程序內真實 cordis 主機重現）抓到兩個「假 ctx 單測全綠、真機失效」的連環 bug：①`ctx.inject(deps, cb)` 的 `sctx` **在 cb 返回後即為 inactive**，cb 內建立的閉包之後再 `sctx.settings` 會拋「cannot get required service "settings" in inactive context」→ 必修慣例：**在 cb 有效期間捕獲服務實例**（`const settings = sctx.settings`），之後只碰實例；②cordis 4 的 teardown 是 **plugin effect 回傳 disposer 函式**，`ctx.dispose()` 不存在，誤呼叫拋 TypeError 令掛載尾段靜默中斷 → 假 ctx 若自己發明該 API，單測會反掩護（本案實際發生過）。兩案皆已修，並加「已載入」日誌斷言與 console 兜底輸出。**規程更新：驗證分三層——解析（裸名 import）→ 掛載（dump-config）→ 心跳（state.json 時間戳／「已載入」日誌），缺角不算通過。**

## 11. 時點快照（2026-09-05 目錄實測，供驗收基準）

### 11.1 R3 推薦候補（3–5 個，均 ≤$1/$1、支援工具、≥256k ctx）

| 候選 id | in/out $/M（OR｜Nous） | 推荐理由 | 風險 |
|---|---|---|---|
| `poolside/laguna-s-2.1` | 0.09/0.18｜0.072/0.144 | 編碼代理特化，Terminal-Bench 2.1 = 70.2%（描述內數據），1M ctx；與 glm-5.3-flash 的 agentic 定位互補 | 通用 intelligence 指數未公開發布（標記 unverified） |
| `meta/muse-spark-1.3-contributor` | 0.10/0.20｜未見 | 旗艦 Muse Spark 1.3（design arena website rank 2）同系列低價檔；1M ctx、943k 輸出、reasoning+tools | contributor 檔為「early-stage」定位，實際能力可能低於旗艦；建議以觀察席位進清單 |
| `meituan/longcat-2.0` | 0.30/1.20✗｜0.24/0.96✓（折後） | 48B/1.6T MoE，repo 級長任務編碼；Nous 現價合格（coding 指數 45.3） | OR 原價輸出 >$1（折扣消失會觸發硬出局）；Nous 限定 |
| `upstage/solar-pro4` | 0.03/0.12｜0.024/0.096 | 極低價、524k ctx、文件/辦公特化（Portal 免費推薦榜首，推測能力受驗證於零價區） | coding 52.7 低於基準線 → 定位為「 ECONOMY 席位」而非等強 |

**近失清單**（規則调整即入選，先記帳不動）：`google/gemini-3.8-flash`（47.1/76.3/41.2，輸出 $1.875——若門檻放寬到 $2 它是第一候補）、`thinkingmachines/inkling-small`（輸出 $1.20）、`openai/gpt-5.6-luna`（輸出 $1.20，Nous 已在使用者手動清單中）。

### 11.2 當前免費池（R1 首輪應收錄）

- OpenRouter：`ling-3.0-flash-sante:free`、`ling-3.0-flash-fin:free`、`nemotron-3.5-lightning:free`（1M ctx）、`inkling-small:free`（多模態 text+image+audio！262k 輸出）、`laguna-s-2.1:free`（32k out 降版）、`laguna-xs-2.1:free`
- Nous： Portal `freeRecommendedModels` = `solar-pro4:free`、`longcat-2.0:free`、`step-3.7-flash:free`（Vision+Compaction 雙推薦）、`ling-3.0-flash-{sante,fin}:free`、`laguna-{s,xs}-2.1:free`——以全目錄零價复核後收錄
- 既有保留：`openrouter/free`（使用者手動加的自動路由器，`imported` 待遇保留）

## 12. 驗收標準

| # | 標準 | 狀態（2026-09-06） |
|---|---|---|
| 1 | 連續 7 天無人值守，pins 5 條在兩個 route 上完整無缺 | ⏳ 觀察期自第二次重啟（plugin 生效）起算；M1 手工套用後已平穩運行 |
| 2 | 免費池輪替 ≤24h 反映到選單（plugin 模式） | ⏳ 同上，待 plugin 生效 |
| 3 | 候補清單 14 天觀察期變動 ≤2 次且有 diff 佐證 | ⏳ 每次變動寫 `CHANGES.md`＋ledger 記帳（behind/cooldown），待觀察 |
| 4 | 故障注入：不寫入壞值、DSH 持續可用、警示可見 | 🟡 離線已證：lastGood 回退（catalog.test）、自驗證拒寫（writer.test）、衝突重試（loop.test）、seam 拒收不崩潰（mount test）；live 拔網演練未做 |
| 5 | 首輪套用僅新增、不刪除 | ✅ 實測通過（新增 32、移除 0；未受管行零刪除；見 M1 里程碑） |

## 13. 待確認問題

| # | 問題 | 建議預設 |
|---|---|---|
| Q1 | ~~「DeepSeek V4 Vision」是哪個 id？~~ | ✅ 已確認（2026-09-05）：`deepseek/deepseek-v4-flash-vision-exp` |
| Q2 | ~~免費池上限？~~ | ✅ 已確認（2026-09-05）：每 provider **10** 個（`freeMax = 10`） |
| Q3 | ~~$1/M 門檻用基準價還是含階梯加價檔位？~~ | ✅ 已確認（2026-09-05）：**規則甲**——第一檔（基準檔位）≤$1 即入選；較高檔位超過 $1 時只在報告註記，不排除 |
| Q4 | 兩家 id 不同時（如 `z-ai/glm-5.2:US` 這種地域後綴），候選池要去重到「每家一條」嗎？ | ✅ 已按預設實作（M1）：家族去重以 `familyOf()`（見 §10 偏離記錄，取代 canonical_slug 分組），同族取最高分、平手取新 |
| Q5 | 是否允許工具動 `agent-default-model`？ | ✅ 已按預設實作（M1）：工具永不讀寫該命名空間；反而受「default 保護」規則保護（被排除時強制保留＋警告） |
| Q6 | Nous 目錄的折後價（20% off `original`）算現價嗎？R3 硬過濾用哪列？ | 🟡 半實作：硬過濾用當前實際價（`pricing`，折後）✅；「退出風險記 `original`」未實作——normalizer 目前丟棄 `original` 欄，列入 M3（折扣消失前可提前預警） |

---

## 附錄 A：pi-ai 內建 openrouter 目錄與線上目錄的差距

- 靜態快照（本機 `.../pi-ai/dist/providers/data/openrouter.json`，generatedAt 2026-08-28）含 333 條目，**已包含全部 5 個 pins（含 `~deepseek/deepseek-v4-flash-latest`）與多數免費變體**；
- 但線上目錄每日有新品（如 `muse-spark-1.3-contributor`、`gpt-6-astra` 系列）、價格改列、免費池輪替——這正是工具必須以線上目錄為準、只在「靜態目錄已有同 id」時省寫元資料的原因。

## 附錄 B：本機證據檔案索引

- 使用者設定：`C:\Users\denny\.dsh\settings.yaml`（另有 `.bak-prenous` 兩份，證明人工維護成本已存在）
- pi-ai 靜態目錄：`C:\Users\denny\AppData\Roaming\npm\node_modules\@deepseek-ai\dsh\node_modules\@earendil-works\pi-ai\dist\providers\data\`（43 個 provider JSON + `.manifest.json`）
- 適配器契約文件：`...\@deepseek-ai\dsh-llm-pi-ai\README.md`（§「Change configuration at runtime」、「Discover models from endpoints」、Known Limitations 第 7 條）
- 設定服務：`...\@deepseek-ai\dsh-settings\lib\types\index.d.ts`（`update/replace/mutate(ns, …, expectedRevision)`、`SettingsConflictError`）
- 原子寫入：`...\@deepseek-ai\dsh-atomic-write`（Windows EBUSY/EACCES 重試內建）
- 插件先例：`C:\Users\denny\.dsh\profiles\web\package.json`（bundles: dsh-plugin-hub、dsh-better-sidebar、dsh-smart-approval）

## 附錄 C：外部參考

- Nous Portal 模型頁（前端）：https://portal.nousresearch.com/models
- Nous 推薦/輪替 API：https://portal.nousresearch.com/api/nous/recommended-models （本文件 §3.2 引用其 2026-09-04 實抓）
- Nous 全目錄 API：https://inference-api.nousresearch.com/v1/models
- OpenRouter 模型頁（前端）：https://openrouter.ai/models?output_modalities=text
- OpenRouter 目錄 API：https://openrouter.ai/api/v1/models （`output_modalities`、`supported_parameters` 過濾已實測）
- 先例：NousResearch/hermes-agent `hermes_cli/models.py`（免費分層、Portal 推薦併入、TTL+disk last-known-good、tools 過濾；見 commit 8553669「remote model catalog manifest for OpenRouter + Nous Portal」）
- OpenRouter 文件：https://openrouter.ai/docs/guides/overview/models
